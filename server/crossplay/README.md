# Crossplay

This folder documents the shared crossplay design for the server.

The server provides:
- available map metadata for both PC and Android players
- available weapon metadata for both PC and Android players
- match setup with selected map and player count

The `CrossplayManager` class in `server/src/main/java/com/codeblub/talkingthroughaid/server` is the central service.
